apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: arbeidssoekerregisteret-internkontroll
  namespace: paw
  labels:
    team: paw
spec:
  image: {{ image }}
  port: 8080
  replicas:
    min: 1
    max: 1
  env:
    - name: PGSSLMODE
      value: "VerifyFull"
    - name: PGSSLROOTCERT
      value: "$(NAIS_DATABASE_ARBEIDSSOEKERREGISTERET_INTERNKONTROLL_INTERNKONTROLL_SSLROOTCERT)"
    - name: PGSSLCERT
      value: "$(NAIS_DATABASE_ARBEIDSSOEKERREGISTERET_INTERNKONTROLL_INTERNKONTROLL_SSLCERT)"
    - name: PGSSLKEY
      value: "$(NAIS_DATABASE_ARBEIDSSOEKERREGISTERET_INTERNKONTROLL_INTERNKONTROLL_SSLKEY)"
    - name: PAA_VEGNE_AV_TOPIC
      value: "paw.arbeidssoker-bekreftelse-paavegneav-v2"
  liveness:
    path: /internal/isAlive
    initialDelay: 10
  readiness:
    path: /internal/isReady
    initialDelay: 10
  startup:
    failureThreshold: 10
    initialDelay: 2
    path: /internal/hasStarted
    periodSeconds: 10
    timeout: 5
  prometheus:
    enabled: true
    path: /internal/metrics
  observability:
    tracing:
      enabled: true
  kafka:
    pool: {{ kafka }}
  resources:
    limits:
      memory: 32Mi
    requests:
      memory: 32Mi
      cpu: 10m
  gcp:
    sqlInstances:
      - type: POSTGRES_18
        tier: db-f1-micro
        databases:
          - name: internkontroll
        highAvailability: false
        pointInTimeRecovery: false
        diskAutoresize: true
        insights:
          enabled: false

