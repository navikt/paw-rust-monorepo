query HentPersonNavn($ident: ID!, $historisk: Boolean = false) {
    hentPerson(ident: $ident) {
        navn(historikk: $historisk) {
            fornavn
            mellomnavn
            etternavn
        }
    }
}